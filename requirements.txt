# Core AI
groq>=0.9.0
openai>=1.0.0

# Dashboard
streamlit>=1.35.0
pydeck>=0.9.0
pandas>=2.0.0

# HTTP (routing API calls)
requests>=2.31.0

# Export
reportlab>=4.0.0  # optional — PDF SitRep export; HTML fallback if missing

# Utilities
python-dotenv>=1.0.0
